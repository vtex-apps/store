import React, { Component } from 'react'

export const Slider = ({ children }) => {
  return <div className="slider-mock">{children}</div>
}

export const Slide = ({ children }) => {
  return <div className="slide-mock">{children}</div>
}

export const Dots = () => {
  return <div className="dots-mock" />
}

export const SliderContainer = ({ children }) => {
  return <div className="slider-container-mock">{children}</div>
}
